CREATE TABLE LIVRO (id INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
autor VARCHAR(255),
avaliacao int NOT NULL,
editora VARCHAR(255),
isbn VARCHAR(255),
paginas int NOT NULL,
titulo VARCHAR(255)); 
